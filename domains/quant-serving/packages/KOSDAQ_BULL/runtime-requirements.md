# Runtime Requirements

- Runtime implementation is not active yet.
- Public list exposure is allowed only as a package-ready model shell until a `LiveQuantModelRuntime` exists.
- Candidate, trade, and report APIs must return empty/error-safe UI states before runtime activation.
