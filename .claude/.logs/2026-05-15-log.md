## 2026-05-15

### 순매수도 API 버그 수정 — FHKST01010900 → frgnmem-trade-estimate 전환

- `FHKST01010900` (`inquire-investor`)는 개별 종목 코드가 필요한 엔드포인트였음 → 지수코드(`0001`) 전달 시 항상 빈 배열 반환
- 외국인 순매수 랭킹 전용 엔드포인트 `frgnmem-trade-estimate` (`FHKST644100C0`)로 전환
- KOSPI 시장코드 `0001` → `2001` 수정, KOSDAQ `fid_cond_mrkt_div_code=J` 버그 수정
- 기관(INSTITUTION) 탭은 API 미발견으로 빈 배열 처리
- 수정 파일:
  - `market-pulse-api/src/main/java/com/marketpulse/domain/stock/dto/ForeignTradeItem.java`
  - `market-pulse-api/src/main/java/com/marketpulse/domain/investor/service/InvestorService.java`

### 순매수 랭킹 스냅샷 저장 구현 (다중 날짜 비교 뷰 지원)

- `ranking_snapshot` 테이블 추가 — 날짜×투자자×거래유형×시장 조합별 TOP20 저장
- `getTradeTop()`: 오늘 → 실시간 API, 과거 날짜 → DB 조회로 분기
- 스케줄러: 매 평일 15:35 자동 저장 (`RankingSnapshotScheduler`)
- 수동 트리거: `POST /api/investor/snapshot?date=YYYYMMDD`
- 생성 파일:
  - `market-pulse-api/src/main/java/com/marketpulse/domain/investor/vo/RankingSnapshotVo.java`
  - `market-pulse-api/src/main/java/com/marketpulse/domain/investor/mapper/RankingSnapshotMapper.java`
  - `market-pulse-api/src/main/resources/mapper/investor/RankingSnapshotMapper.xml`
  - `market-pulse-api/src/main/java/com/marketpulse/global/config/RankingSnapshotScheduler.java`
- 수정 파일:
  - `market-pulse-api/src/main/resources/data.sql` (ranking_snapshot DDL 추가)
  - `market-pulse-api/src/main/java/com/marketpulse/domain/investor/service/InvestorService.java`
  - `market-pulse-api/src/main/java/com/marketpulse/domain/investor/controller/InvestorController.java`

### 순매수도 (NetBuyingList) 풀스택 완성

- 스냅샷 날짜 조회 API 추가: `GET /api/investor/snapshot/dates?investorType&tradeType&market`
  - `RankingSnapshotMapper`: `findAvailableDates` 추가 (DISTINCT snap_date, YYYYMMDD 포맷)
  - `InvestorService`: `getAvailableDates` 추가
  - `InvestorController`: `/snapshot/dates` GET 엔드포인트 추가
- 프론트엔드 (`NetBuyingList/index.tsx`) 개선:
  - `snapshotDates: Set<string>` 상태 추가 — 필터 변경 시 저장된 날짜 목록 조회
  - 기관(INSTITUTION) 선택 시 API 호출 스킵, 즉시 빈 상태 표시
  - 스마트 빈 상태: 기관→"준비 중" / 오늘 데이터 없음→"집계 전 또는 휴장일" / 과거 미저장→"스냅샷 없음 · 날짜 자동 저장 안내"
  - 단일 날짜 헤더에 날짜 상태 표시 (실시간 / 저장됨 / 미저장)
  - 다중 날짜 컬럼 헤더에 날짜별 상태 인디케이터 (실시간/저장됨/미저장)
  - 다중 날짜 모드 기관 선택 시 상단 배너 표시
- 수정 파일:
  - `market-pulse-api/src/main/java/com/marketpulse/domain/investor/mapper/RankingSnapshotMapper.java`
  - `market-pulse-api/src/main/resources/mapper/investor/RankingSnapshotMapper.xml`
  - `market-pulse-api/src/main/java/com/marketpulse/domain/investor/service/InvestorService.java`
  - `market-pulse-api/src/main/java/com/marketpulse/domain/investor/controller/InvestorController.java`
  - `market-pulse-web/src/pages/NetBuyingList/index.tsx`

### DB 테이블 생성 필요 (psql 직접 실행)

```sql
CREATE TABLE IF NOT EXISTS ranking_snapshot (
    id             BIGSERIAL    PRIMARY KEY,
    snap_date      DATE         NOT NULL,
    investor_type  VARCHAR(20)  NOT NULL,
    trade_type     VARCHAR(10)  NOT NULL,
    market         VARCHAR(10)  NOT NULL,
    rank           INTEGER      NOT NULL,
    stock_code     VARCHAR(10)  NOT NULL,
    stock_name     VARCHAR(100) NOT NULL,
    net_buy_amount BIGINT       NOT NULL DEFAULT 0,
    net_buy_volume BIGINT       NOT NULL DEFAULT 0,
    CONSTRAINT uq_ranking_snapshot UNIQUE (snap_date, investor_type, trade_type, market, rank)
);
CREATE INDEX IF NOT EXISTS idx_ranking_snapshot_date ON ranking_snapshot (snap_date);
```

### NetBuyingList UI 개선 + 지분률 추가

- 단일 날짜 테이블 컬럼 단순화: 현재가·등락률 제거 → 순위/종목명/대금(억)/수량만 표시
- 종목 행 클릭 시 상세 모달(`StockModal`) 추가
  - 현재가·등락률·외국인 지분률·순매수대금·수량 표시
  - 지분률은 외국인 선택 시에만, 실시간 데이터에서만 제공
  - 메모 입력창 (날짜+시장 기준 investor_memo API 연동, 로그인 필요)
  - 종목 상세 페이지 이동 링크 (`/stock/:code`)
- 하단 메모 섹션 추가 (단일 날짜 뷰 전용) — InvestorTrend와 동일한 UX
- KIS API `frgn_hldn_qty_rt` 필드로 외국인 지분률 수집
- 수정 파일:
  - `market-pulse-api/src/main/java/com/marketpulse/domain/stock/dto/ForeignTradeItem.java`
  - `market-pulse-api/src/main/java/com/marketpulse/domain/investor/dto/TradeTopResponseDto.java`
  - `market-pulse-api/src/main/java/com/marketpulse/domain/investor/service/InvestorService.java`
  - `market-pulse-web/src/pages/NetBuyingList/index.tsx`
  - `.claude/.back/back.md`
  - `.claude/.front/front.md`

### NetBuyingList 항상-타임라인 + 우측 고정 합계 컬럼

- 단일/다중 날짜 토글 제거 → 항상 타임라인 뷰로 통합
- 날짜 범위 내 영업일을 `getWeekdays(start, end)`로 자동 생성, 날짜별 API 병렬 호출
- 기존 5영업일마다 주간합계 컬럼 삽입 방식 → 조회기간 전체 누적 합계 단일 컬럼으로 변경
- 합계 컬럼 우측 고정(sticky right): `borderCollapse:"separate", borderSpacing:0` 필수
  - 순위 44px 좌측 고정, 합계 3컬럼(종목명 110+대금 78+수량 68) 우측 고정
  - 좌측 그림자: `box-shadow: -4px 0 6px -4px rgba(0,0,0,0.3)`
- `calcRangeTotal(dates, dataMap)`: 전 날짜 집계 → 순매수대금 기준 TOP20 재랭킹 (`useMemo`)
  - 부분 로딩 중에도 완료된 날짜 기준으로 즉시 표시
- 수정 파일:
  - `market-pulse-web/src/pages/NetBuyingList/index.tsx`

### NetBuyingList 주 이동 버튼 (날짜 UI 개편)

- 1주/2주/1개월 프리셋 버튼 제거
- 날짜 선택 앞뒤로 주 이동 버튼 추가: `[← 이전주] [시작일] ~ [종료일] [다음주 →] [오늘]`
- `shiftWeek(delta)`: 시작+종료를 동시에 5영업일씩 이동 (주말 자동 스킵)
- `nWeekdaysAfter(n, from)` 헬퍼: n영업일 이후 날짜 반환
- `resetToToday()`: 종료일→오늘 고정, 기간 폭(영업일 수) 유지
- 기본 범위: 오늘 기준 최근 10영업일 (9영업일 전 ~ 오늘)
- 수정 파일:
  - `market-pulse-web/src/pages/NetBuyingList/index.tsx`
  - `.claude/.front/front.md`
