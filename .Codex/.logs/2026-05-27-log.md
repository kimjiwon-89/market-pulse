## 2026-05-27

### Live quant simulation planning
- Defined the replacement direction for `/quant`: model list, model detail, and report views instead of the old backtesting-first UX.
- Locked decisions: KRW 100M seed per visible model, model-owned position sizing, 1-minute MVP monitoring, deterministic post-close reports without AI.
- Added post-exit outcome tracking at 1 trading day, 7 days, 2 weeks, 1 month, and 6 months so models can generate auditable learning feedback.
- Expanded tracking to watched-but-not-traded assets and required completed checkpoint analysis to appear in daily/weekly reports.
- Files changed: `.Codex/plans/2026-05-27_live-quant-simulation.html`, `.Codex/plans/2026-05-27_live-quant-simulation-spec.md`

### Live quant simulation MVP implementation
- Added `/api/quant/live/*` DTOs, controller, deterministic live simulation service, rule-based report writer, and focused backend tests.
- Replaced `/quant` frontend with model list, model detail tabs, trade/exit/learning views, and report detail checkpoint analysis.
- Added legacy quant model archive HTML for the backtesting-first model family.
- Files changed: `market-pulse-api/src/main/java/com/marketpulse/domain/quant/live/`, `market-pulse-api/src/test/java/com/marketpulse/domain/quant/live/LiveQuantSimulationServiceTest.java`, `market-pulse-web/src/pages/QuantDashboard/index.tsx`, `.Codex/reports/2026-05-27_legacy-quant-model-archive.html`

### Live quant realtime data cleanup
- Removed fake live simulation rows from model positions, candidates, trades, watched assets, and report summaries.
- Kept only Bull v4 as runnable; index, sideways, and bear models now expose `SERVICE_PREPARING` with zero counts until real model services are wired.
- Bull v4 now uses KIS-backed realtime quote availability only as a live status probe and does not invent trades before real model decisions are stored.
- Files changed: `market-pulse-api/src/main/java/com/marketpulse/domain/quant/live/`, `market-pulse-api/src/test/java/com/marketpulse/domain/quant/live/LiveQuantSimulationServiceTest.java`, `market-pulse-web/src/pages/QuantDashboard/index.tsx`

### Models mobile detail UX
- Split `/quant` into a model list page and `/quant/models/:modelCode` into a dedicated model detail page.
- Reworked the mobile flow so model cards navigate to detail instead of stacking full detail content under the list.
- Kept reports on the list page and made detail tabs sticky for mobile scanning.
- Files changed: `market-pulse-web/src/app/router.tsx`, `market-pulse-web/src/pages/QuantDashboard/index.tsx`

### Models PC section layout
- Split the Models list into a top index-regime section and a separate trading-model section.
- Kept all model cards as navigation entry points to `/quant/models/:modelCode` with no inline detail preview on the list page.
- Verified the PC list shows both sections and no `Bull v4 모델 상세` preview content.
- Files changed: `market-pulse-web/src/pages/QuantDashboard/index.tsx`

### Report navigation split
- Removed the list-page Models heading so the section headers carry the page structure.
- Moved reports to a dedicated `/reports` route with period, model, date, and provisional asset filters.
- Added Report to desktop and mobile navigation, and changed the detail back action to `← 뒤로가기`.
- Files changed: `market-pulse-web/src/app/router.tsx`, `market-pulse-web/src/components/common/Nav.tsx`, `market-pulse-web/src/components/common/BottomNav.tsx`, `market-pulse-web/src/pages/QuantDashboard/index.tsx`

### Navigation cleanup
- Removed the overlapping investor trend navigation item from desktop and mobile menus.
- Kept the existing investor route/page code in place so direct URLs do not break.
- Files changed: `market-pulse-web/src/components/common/Nav.tsx`, `market-pulse-web/src/components/common/BottomNav.tsx`

### Back button cleanup
- Removed custom back buttons from the model detail and stock detail pages to match the app-level navigation pattern.
- Verified there are no remaining `뒤로`/`navigate(-1)` controls in the frontend source.
- Files changed: `market-pulse-web/src/pages/QuantDashboard/index.tsx`, `market-pulse-web/src/pages/StockDetail/index.tsx`

### Historical Bull replay and reports
- Added a real `market_daily_price` based historical replay fallback for Bull v4 so the model page is populated from stored market data instead of hard-coded samples.
- Exposed replay trades, watched assets, weekly reports, and May 2026 daily reports through the live quant API.
- Set the Report page to open on weekly reports first, with daily May reports available through the period filter.
- Files changed: `market-pulse-api/src/main/java/com/marketpulse/domain/quant/live/`, `market-pulse-api/src/main/java/com/marketpulse/domain/quant/mapper/MarketDailyPriceMapper.java`, `market-pulse-api/src/main/resources/mapper/quant/MarketDailyPriceMapper.xml`, `market-pulse-api/src/test/java/com/marketpulse/domain/quant/live/LiveQuantSimulationServiceTest.java`, `market-pulse-web/src/pages/QuantDashboard/index.tsx`

### Report paging and detail route
- Reworked the Report list into a paged table with 20/50/100 page-size controls and visible title/date/model/period/performance columns.
- Changed report clicks to navigate to `/reports/:reportId` and render a dedicated detail page.
- Fixed weekly report detail generation so weekly summaries keep `WEEKLY` period and weekly titles.
- Files changed: `market-pulse-web/src/pages/QuantDashboard/index.tsx`, `market-pulse-web/src/app/router.tsx`, `market-pulse-api/src/main/java/com/marketpulse/domain/quant/live/service/RuleBasedReportWriter.java`, `market-pulse-api/src/main/java/com/marketpulse/domain/quant/live/service/LiveQuantSimulationService.java`, `market-pulse-api/src/test/java/com/marketpulse/domain/quant/live/LiveQuantSimulationServiceTest.java`

### Report template sections
- Changed report detail generation from raw checkpoint listing to rule-based template sections.
- Added sections for raw candidates, entry candidates, entry/reject reason, holding changes, exit changes, expected return, risk flags, and next-day watch points.
- Moved checkpoint analysis to a supporting evidence log below the main report body.
- Files changed: `market-pulse-api/src/main/java/com/marketpulse/domain/quant/live/dto/`, `market-pulse-api/src/main/java/com/marketpulse/domain/quant/live/service/RuleBasedReportWriter.java`, `market-pulse-api/src/test/java/com/marketpulse/domain/quant/live/LiveQuantSimulationServiceTest.java`, `market-pulse-web/src/pages/QuantDashboard/index.tsx`

### Professional report detail layout
- Reworked `/reports/:reportId` into a document-style report with header metadata, executive summary, KPI strip, numbered review sections, and evidence log table.
- Restored Korean labels for report metadata, KPI labels, and evidence table headers.
- Verified the detail page renders the report structure without browser console errors.
- Files changed: `market-pulse-web/src/pages/QuantDashboard/index.tsx`

### Model summary dashboard expansion
- Expanded the model summary tab from four metrics into a dashboard with current evaluated seed, monthly trading summary, candidate expected return, latest daily/weekly reports, weekly candidates, and operations memo.
- Wired the model detail page to fetch model detail plus daily/weekly report summaries for report cards.
- Removed the slow H20 replay pre-query from the live replay provider so the model detail can load from the faster stored daily replay baseline.
- Files changed: `market-pulse-web/src/pages/QuantDashboard/index.tsx`, `market-pulse-api/src/main/java/com/marketpulse/domain/quant/live/service/MarketDailyPriceReplayProvider.java`

### Report list pagination and date format
- Changed Report list pagination from previous/next controls to numbered page buttons plus next.
- Formatted daily report dates as `yyyy.mm.dd` and weekly report keys as their ISO week start date in the same format.
- Verified the Report list no longer displays raw weekly keys like `2026W22`.
- Files changed: `market-pulse-web/src/pages/QuantDashboard/index.tsx`
