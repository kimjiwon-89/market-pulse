# 기본 설정 (배포용) - 포트, MyBatis 등
# DB 연결 정보는 conf/application-local.yml 에서 로드
server:
  port: 8080

spring:
  profiles:
    active: local

mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
  type-aliases-package: com.marketpulse.domain