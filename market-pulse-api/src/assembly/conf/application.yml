# 기본 설정 (배포용) - 포트, MyBatis 등
# DB 연결 정보는 conf/application-datasource.yml 에서 로드
server:
  port: 8080

mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
  type-aliases-package: com.marketpulse.domain