# Prod Infra

Infrastructure and deployment configuration.

Responsibilities:

- Docker/compose configuration
- deployment scripts
- GitHub workflow definitions
- environment templates
- runbooks

Production deploy/mutation requires explicit approval in the current user message.
